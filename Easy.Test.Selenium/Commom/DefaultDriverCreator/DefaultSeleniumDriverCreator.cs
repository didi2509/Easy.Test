using Easy.Test.Selenium.Base;

namespace Easy.Test.Selenium
{
    /// <summary>
    /// Criador padrão de drivers do selenium, para sobrescrever apenas o caminho dos drivers utilize a sobrescrita de métodos (override) na classe (partial)
    /// </summary>
    public partial class DefaultSeleniumDriverCreator : SeleniumDriverCreatorBase
    {
      
    }
}
